# Normalize all text files to LF in repo
* text=auto

# Force CRLF for specific files (e.g., Windows batch scripts)
*.bat text eol=crlf

# Force LF for shell scripts
*.sh text eol=lf

# Mark binary files to avoid corruption
*.png binary
*.jpg binary
